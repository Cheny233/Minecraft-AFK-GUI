<Window x:Class="Minecraft_AFK_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minecraft_AFK_GUI"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{x:Null}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        
        mc:Ignorable="d"
        Title="Minecraft-AFK-GUI" Height="450" Width="800" AllowsTransparency="True" WindowStyle="None" ResizeMode="NoResize">


    <Grid Margin="10">
        <Grid.Effect>
            <DropShadowEffect Color="Black" Direction="0" ShadowDepth="0" Opacity="0.7"  BlurRadius="10"/>
        </Grid.Effect>

        <Border CornerRadius="10,10,10,10" Background="{DynamicResource MaterialDesignPaper}" Height="Auto">
            <TextBlock TextWrapping="Wrap" Margin="672,403,45,11"><Run Language="zh-cn" Text="深色模式"/></TextBlock>
        </Border>
        <Border MouseMove="Drag_MouseMove" BorderThickness="0" BorderBrush="Gray" Margin="0,0,0,405" CornerRadius="10,10,0,0" Background="#FF52C7B8"/>

        <Button
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
            ToolTip="MaterialDesignFloatingActionMiniLightButton" Margin="671,5,69,385"
            Click="MinClick">
            <materialDesign:PackIcon Kind="minus" />
        </Button>

        <Button
            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
            Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
            ToolTip="MaterialDesignFloatingActionMiniLightButton" Margin="723,5,17,385"
            Click="CloseClick">
            <materialDesign:PackIcon Kind="close" />
        </Button>
        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}" Margin="734,197,12,-197" RenderTransformOrigin="0.995,0.498" IsChecked="True"  Checked="To_Dark_Mode" Unchecked="To_Light_Mode"/>
    </Grid>
</Window>
